#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '("app/server") :silent t))

(defpackage :ros.script.app
  (:use :cl))
(in-package :ros.script.app)


(defun main (&rest argv)
  (declare (ignorable argv))
  (app/server:start)
  (loop do (sleep 5)))

;;; vim: set ft=lisp lisp:
